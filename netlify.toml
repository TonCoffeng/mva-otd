[build]
  publish = "public"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# SPA-fallback: alle routes naar index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
